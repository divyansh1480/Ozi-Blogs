{
  "name": "ozi-blogs-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  },
  "packageManager": "npm@10.0.0"
}
